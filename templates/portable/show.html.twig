{% extends 'base.html.twig' %}

{% block title %}
	{{portable.name}}
{% endblock %}

{% block content %}
	<section class="section gallery">
		<div class="container">
		<a class="btn btn-second" href="#" onclick="history.back()"><i class="fa-solid fa-arrow-left"></i> Retour</a>
		<br>
			<div class="row mt-5 pt-2 ">
				<div class="col-md-5 mb-4 mb-md-0">
					<div class="product_image">
						<img src="/uploads/{{ portable.illustration}}" alt="{{portable.name}}">
					</div>
				</div>

				<div class="col-lg-6 col-md-7 offset-lg-1">
					<h1 class="mb-4">{{portable.name}}</h3>
					<h3>{{portable.subtitle}}</h3>
					<p class="price py-4">{{(portable.price/100) |number_format(2, ',', '.')}} €</p>
					<hr>
					<p>{{portable.description}}</p>
					<a href="{{path('add_to_cart', {'id' : portable.id})}}" class="btn btn-main">Ajouter au panier</a>
				</div>
			</div>
		</div>
	</section>


{% endblock %}
